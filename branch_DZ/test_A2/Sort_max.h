#ifndef SORT_MAX_H
#define SORT_MAX_H

#include <stddef.h>

/**
 * @brief Sorts an array of integers even ones in ascending order.
 *
 * @param a An array of integers to be sorted.
 * @param n The number of elements in the array.
 */

void Sort_max(int a[], size_t n);

#endif // SORT_MAX_H
