#ifndef SORT_MIN_H
#define SORT_MIN_H

#include <stddef.h>

/**
 * @brief Sorts an array of integers even ones in descending order.
 *
 * @param a An array of integers to be sorted.
 * @param n The number of elements in the array.
 */

void Sort_min(int a[], size_t n);

#endif // SORT_MIN_H
